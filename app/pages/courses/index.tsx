import React from 'react';
import RootLayout from '../../layout';

const Courses: React.FC = () => {
  return (
    <RootLayout>
      <h2>コース一覧</h2>
      <p>ここでは、利用可能なコースの一覧を表示します。</p>
</RootLayout>
  );
};

export default Courses;
